#include <iostream>
#include <vector>

using namespace std;

int main() {
    // input
    int n, k, x;
    cin >> n >> k >> x;
    vector<int> a(n);
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }
    vector<int> b(n);
    for (int i = 0; i < n; i++) {
        cin >> b[i];
    }

    // algoritma

    // output

    cout << 0 << endl;

    return 0;
}
