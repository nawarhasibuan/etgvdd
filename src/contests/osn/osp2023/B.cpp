#include <algorithm>
#include <iostream>
#include <vector>

using namespace std;

int main() {
    // input
    int n, m, k;
    cin >> n >> m >> k;
    vector<int> a(n);
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }

    // algoritma
    int ans = 0;

    // output
    cout << ans << endl;

    return 0;
}
